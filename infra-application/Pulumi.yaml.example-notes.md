# Pulumi Local Notes

- Stack por defecto sugerido: `dev`.
- Este módulo espera que LocalStack esté accesible como `http://localstack:4566` dentro de la red Docker.
- Para usar host local en vez de red Docker, cambia endpoints en `Pulumi.dev.yaml` a `http://host.docker.internal:4566`.
